# Server
NODE_ENV=development
PORT=8000

# Database (will add Neon URL later)
DATABASE_URL="postgresql://user:password@localhost:5432/realestate?schema=public"

# JWT Secrets - REQUIRED (generate strong 256-bit secrets)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Must be at least 32 characters long
# SECURITY: Use different secrets for access and refresh tokens
# NEVER commit your actual secrets to git!
JWT_ACCESS_SECRET=GENERATE_YOUR_OWN_32_CHAR_SECRET_HERE
JWT_REFRESH_SECRET=GENERATE_YOUR_OWN_32_CHAR_SECRET_HERE

# CORS
FRONTEND_URL=http://localhost:3000

# Master Encryption Key - REQUIRED (for per-user API credential encryption)
# Generate with: openssl rand -hex 32
# OR: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Must be exactly 64 hex characters (32 bytes)
# SECURITY: This key is used to derive per-user encryption keys for API credentials
# NEVER commit your actual encryption key to git!
# If this key is lost, all encrypted API credentials will be unrecoverable
MASTER_ENCRYPTION_KEY=GENERATE_YOUR_OWN_64_CHAR_HEX_KEY_HERE

# =============================================================================
# AI & VOICE CONFIGURATION (Phase 1-2)
# =============================================================================

# OpenAI Configuration (AI Chatbot, Lead Scoring, Message Enhancement)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_ORG_ID=org-your-openai-org-id-here
OPENAI_MODEL=gpt-4-turbo-preview

# Vapi.ai Configuration (Voice AI Calls)
# Get from: https://vapi.ai/dashboard
VAPI_API_KEY=your-vapi-api-key-here
VAPI_WEBHOOK_SECRET=your-vapi-webhook-secret-here

# =============================================================================
# BILLING CONFIGURATION (Phase 4)
# =============================================================================

# Stripe Configuration (Subscriptions & Payments)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key-here
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret-here

# =============================================================================
# CACHING (Phase 5 - Optional)
# =============================================================================

# Redis Configuration (for caching, optional)
REDIS_URL=redis://localhost:6379
REDIS_ENABLED=false

# API Keys (Users will provide their own - BYOK)
# These are just for reference, users add them in the app
